// Omniflow batch 311-330 — PARTIAL COMPLETION PASS: sidebar fixes + language +
// identity stitching + auto-escalation + intent classifier + action requests.
// Run from the folder that contains BOTH repos (Omniflow/ and OmniFlow-Control-Plane/):
//   node add_batch_311_330.mjs
// SIDEBAR FIXES: all nine empty nav icons get real glyphs (Overview house, WhatsApp
// phone, Configure AI asterism, Conversations envelope, Customers smiley,
// Automations zap, Analytics bullseye, Business profile card, Knowledge base grid)
// and Settings gets the gear; BOTH sidebar containers (desktop aside + mobile
// drawer) become min-h-0 flex-1 overflow-y-auto so all 21 items scroll instead of
// hiding below the fold.
// CP: NEW portal_contacts.py — workspace reply language (auto/en/ur/roman) with
// GET/PUT, per-contact language GET/POST (auto deletes the row), a deterministic
// script/marker language detector (Arabic script -> ur, Roman-Urdu marker hits ->
// roman, else en), identity stitching (portal_contact_identities with a sorted
// deterministic identity_key, /contacts/link + /identities), and the autonomous
// ACTION REQUEST surface (portal_action_requests: cancel_order / change_address /
// refund_request created against a conversation, listed per contact/status,
// resolved done/declined — every step audited). portal_insights.py gains
// GET /insights/intent (ordered regex rules -> refund/order_status/complaint/
// pricing/delivery/greeting/other + entity extraction: phones, #order ids,
// emails). portal_growth.py record_kb_gap now returns the gap count via
// INSERT..RETURNING and auto-ESCALATES on the 2nd gap: probes portal_team_members
// (to_regclass-guarded), assigns the conversation to the first member, audits
// bot.escalated. connector_api.py calls maybe_detect_language on inbound (once per
// contact, ON CONFLICT DO NOTHING). customer_profile gains language,
// linked_channels and actions sections (all to_regclass-guarded).
// Web: portal.ts clients for all of the above + CustomerProfile gains
// language/linkedChannels/actions; EIGHT BFF routes (depth law: workspace 6,
// contacts/* 6, detect 7, insights/intent 6, conversations/[id]/actions 7,
// contacts/actions 6, contacts/actions/[id] 7); the 360 page shows a language
// chip, Linked: rows and an Action requests section; the Activity page gains an
// "Escalations" family chip (bot. prefix).
// Tests (rig, not this patcher): test_partial_completion.py 78,
// test_partial_completion_ui.py 51, test_customer_360.py extended to 33.
// Idempotent: re-run reports "already done" per block. Backups: *.pre_b311330.bak
import fs from "fs";
import { createRequire } from "module";
const require = createRequire(import.meta.url);

const BACKUP_TAG = ".pre_b311330.bak";
let applied = 0;
let already = 0;
let warnings = 0;

function compilePython(pathArg) {
  const { spawnSync } = require("child_process");
  for (const py of ["python3", "python", "py"]) {
    const probe = spawnSync(py, ["--version"], { encoding: "utf8" });
    if (probe.status !== 0) continue;
    const check = spawnSync(py, ["-c", "import sys;sys.exit(0 if sys.version_info[0]>=3 else 1)"], { encoding: "utf8" });
    if (check.status !== 0) continue;
    const result = spawnSync(py, ["-c", "import py_compile,sys;py_compile.compile(sys.argv[1],doraise=True);sys.exit(0)", pathArg], { encoding: "utf8" });
    return result.status === 0;
  }
  console.log("  (python not found — skipped compile guard)");
  return true;
}

function applySwaps(repoPath, marker, isCp, swaps) {
  if (!fs.existsSync(repoPath)) {
    console.log("SKIP (file not found): " + repoPath);
    warnings++;
    return;
  }
  const original = fs.readFileSync(repoPath, "utf8").replace(/\r\n/g, "\n");
  if (original.includes(marker)) {
    console.log("= " + repoPath + " (already patched)");
    already++;
    return;
  }
  const backup = repoPath + BACKUP_TAG;
  if (!fs.existsSync(backup)) fs.copyFileSync(repoPath, backup);
  let updated = original;
  let ok = true;
  let swapIndex = 0;
  for (const [frm, to] of swaps) {
    swapIndex++;
    if (!updated.includes(frm)) {
      const hits = original.split(frm.slice(0, 40)).length - 1;
      console.log(
        "  ? " + repoPath + " :: anchor NOT FOUND — report this" +
        " (swap " + swapIndex + "/" + swaps.length +
        ", anchor " + frm.length + " chars, starts: " +
        JSON.stringify(frm.slice(0, 60)) +
        ", " + hits + " partial hits)"
      );
      ok = false;
      warnings++;
      break;
    }
    updated = updated.replace(frm, to);
  }
  if (!ok) return;
  fs.writeFileSync(repoPath, updated, "utf8");
  if (isCp && !compilePython(repoPath)) {
    fs.copyFileSync(backup, repoPath);
    console.log("FAIL (compile failed, restored): " + repoPath);
    warnings++;
    return;
  }
  console.log("+ " + repoPath + " (" + swaps.length + ")");
  applied++;
}

function writeNew(repoPath, marker, isCp, content) {
  if (!fs.existsSync(repoPath)) {
    fs.mkdirSync(repoPath.replace(/\/[^/]*$/, ""), { recursive: true });
    fs.writeFileSync(repoPath, content, "utf8");
    if (isCp && !compilePython(repoPath)) {
      console.log("FAIL (compile failed): " + repoPath);
      warnings++;
      return;
    }
    console.log("+ " + repoPath + " (new file)");
    applied++;
    return;
  }
  const original = fs.readFileSync(repoPath, "utf8").replace(/\r\n/g, "\n");
  if (original.includes(marker)) {
    console.log("= " + repoPath + " (already patched)");
    already++;
    return;
  }
  console.log("  ? " + repoPath + " exists WITHOUT marker — report this");
  warnings++;
}






const CP = "OmniFlow-Control-Plane";
const WEB = "Omniflow";

applySwaps(CP + "/app.py", "portal_churn_bp", true, [["from portal_reco import bp as portal_reco_bp  # noqa: E402\nfrom portal_checkout import public_bp as portal_checkout_public_bp  # noqa: E402", "from portal_reco import bp as portal_reco_bp  # noqa: E402\nfrom portal_churn import bp as portal_churn_bp  # noqa: E402\nfrom portal_checkout import public_bp as portal_checkout_public_bp  # noqa: E402"], ["aux_app.register_blueprint(portal_reco_bp)\naux_app.register_blueprint(portal_checkout_public_bp)", "aux_app.register_blueprint(portal_reco_bp)\naux_app.register_blueprint(portal_churn_bp)\naux_app.register_blueprint(portal_checkout_public_bp)"]]);
applySwaps(WEB + "/lib/omniflow/portal.ts", "getChurnScore", false, [["    catalogItems: typeof row.catalog_items === \"number\" ? row.catalog_items : 0,\n  };\n}\n\nexport async function deleteSequence(", "    catalogItems: typeof row.catalog_items === \"number\" ? row.catalog_items : 0,\n  };\n}\n\nexport interface ChurnSignals {\n  lastInboundDays: number | null;\n  unansweredDays: number | null;\n  openCartDays: number | null;\n  ordersLast30d: number;\n  ordersPrior30d: number;\n  paidOrders: number;\n  lastOrderDays: number | null;\n}\n\nexport interface ChurnScore {\n  contactId: string;\n  score: number;\n  tier: string;\n  reasons: string[];\n  signals: ChurnSignals;\n}\n\nexport interface ChurnRadarEntry {\n  contactId: string;\n  name: string;\n  score: number;\n  tier: string;\n  reasons: string[];\n}\n\nexport interface ChurnRadar {\n  contacts: ChurnRadarEntry[];\n  scored: number;\n  counts: Record<string, number>;\n}\n\nexport interface ChurnTopReason {\n  reason: string;\n  count: number;\n}\n\nexport interface ChurnReport {\n  scored: number;\n  avgScore: number | null;\n  tiers: Record<string, number>;\n  topReasons: ChurnTopReason[];\n}\n\nfunction churnSignalsOf(row: Record<string, unknown>): ChurnSignals {\n  const raw =\n    row.signals !== null && typeof row.signals === \"object\"\n      ? (row.signals as Record<string, unknown>)\n      : {};\n  const num = (key: string): number =>\n    typeof raw[key] === \"number\" ? (raw[key] as number) : 0;\n  const opt = (key: string): number | null =>\n    typeof raw[key] === \"number\" ? (raw[key] as number) : null;\n  return {\n    lastInboundDays: opt(\"last_inbound_days\"),\n    unansweredDays: opt(\"unanswered_days\"),\n    openCartDays: opt(\"open_cart_days\"),\n    ordersLast30d: num(\"orders_last_30d\"),\n    ordersPrior30d: num(\"orders_prior_30d\"),\n    paidOrders: num(\"paid_orders\"),\n    lastOrderDays: opt(\"last_order_days\"),\n  };\n}\n\nfunction churnReasonsOf(row: Record<string, unknown>): string[] {\n  const raw = row.reasons;\n  return Array.isArray(raw)\n    ? raw.filter((reason): reason is string => typeof reason === \"string\")\n    : [];\n}\n\nexport async function getChurnScore(\n  accessToken: string,\n  contact: string\n): Promise<ChurnScore | null> {\n  let response: Response;\n  try {\n    response = await portalRequest(\n      accessToken,\n      \"api/v1/portal/churn/score?contact=\" + encodeURIComponent(contact)\n    );\n  } catch (error) {\n    assertNotAuthError(error);\n    return null;\n  }\n  if (response.status === 404 || response.status === 501) return null;\n  if (response.status === 401) throw new ControlPlaneRequestError(401, \"unauthorized\");\n  if (!response.ok) return null;\n  const payload: unknown = await response.json().catch(() => null);\n  if (payload === null || typeof payload !== \"object\") return null;\n  const row = payload as Record<string, unknown>;\n  return {\n    contactId: typeof row.contact_id === \"string\" ? row.contact_id : contact,\n    score: typeof row.score === \"number\" ? row.score : 0,\n    tier: typeof row.tier === \"string\" ? row.tier : \"healthy\",\n    reasons: churnReasonsOf(row),\n    signals: churnSignalsOf(row),\n  };\n}\n\nexport async function getChurnRadar(\n  accessToken: string,\n  limit = 10\n): Promise<ChurnRadar | null> {\n  let response: Response;\n  try {\n    response = await portalRequest(\n      accessToken,\n      \"api/v1/portal/churn/radar?limit=\" + encodeURIComponent(String(limit))\n    );\n  } catch (error) {\n    assertNotAuthError(error);\n    return null;\n  }\n  if (response.status === 404 || response.status === 501) return null;\n  if (response.status === 401) throw new ControlPlaneRequestError(401, \"unauthorized\");\n  if (!response.ok) return null;\n  const payload: unknown = await response.json().catch(() => null);\n  if (payload === null || typeof payload !== \"object\") return null;\n  const row = payload as Record<string, unknown>;\n  const rawContacts = Array.isArray(row.contacts) ? row.contacts : [];\n  const counts =\n    row.counts !== null && typeof row.counts === \"object\"\n      ? (row.counts as Record<string, unknown>)\n      : {};\n  const tierCounts: Record<string, number> = {};\n  for (const [key, value] of Object.entries(counts)) {\n    if (typeof value === \"number\") tierCounts[key] = value;\n  }\n  return {\n    contacts: rawContacts\n      .filter((item): item is Record<string, unknown> =>\n        item !== null && typeof item === \"object\")\n      .map((item) => ({\n        contactId: typeof item.contact_id === \"string\" ? item.contact_id : \"\",\n        name: typeof item.name === \"string\" ? item.name : \"\",\n        score: typeof item.score === \"number\" ? item.score : 0,\n        tier: typeof item.tier === \"string\" ? item.tier : \"healthy\",\n        reasons: churnReasonsOf(item),\n      })),\n    scored: typeof row.scored === \"number\" ? row.scored : 0,\n    counts: tierCounts,\n  };\n}\n\nexport async function getChurnReport(\n  accessToken: string\n): Promise<ChurnReport | null> {\n  let response: Response;\n  try {\n    response = await portalRequest(accessToken, \"api/v1/portal/churn/report\");\n  } catch (error) {\n    assertNotAuthError(error);\n    return null;\n  }\n  if (response.status === 404 || response.status === 501) return null;\n  if (response.status === 401) throw new ControlPlaneRequestError(401, \"unauthorized\");\n  if (!response.ok) return null;\n  const payload: unknown = await response.json().catch(() => null);\n  if (payload === null || typeof payload !== \"object\") return null;\n  const row = payload as Record<string, unknown>;\n  const rawReasons = Array.isArray(row.top_reasons) ? row.top_reasons : [];\n  const tiers =\n    row.tiers !== null && typeof row.tiers === \"object\"\n      ? (row.tiers as Record<string, unknown>)\n      : {};\n  const tierCounts: Record<string, number> = {};\n  for (const [key, value] of Object.entries(tiers)) {\n    if (typeof value === \"number\") tierCounts[key] = value;\n  }\n  return {\n    scored: typeof row.scored === \"number\" ? row.scored : 0,\n    avgScore: typeof row.avg_score === \"number\" ? row.avg_score : null,\n    tiers: tierCounts,\n    topReasons: rawReasons\n      .filter((item): item is Record<string, unknown> =>\n        item !== null && typeof item === \"object\")\n      .map((item) => ({\n        reason: typeof item.reason === \"string\" ? item.reason : \"\",\n        count: typeof item.count === \"number\" ? item.count : 0,\n      })),\n  };\n}\n\nexport async function deleteSequence("]]);
writeNew(WEB + "/app/api/omniflow/portal/churn/score/route.ts", "getChurnScore", false, "import {\n  getChurnScore,\n  requirePortalAccessToken,\n} from \"../../../../../../lib/omniflow/portal\";\nimport {\n  noStoreHeaders,\n  safeJson,\n} from \"../../../../../../lib/omniflow/request-security\";\nimport { ControlPlaneRequestError } from \"../../../../../../lib/omniflow/control-plane\";\n\n\nexport async function GET(request: Request) {\n  const accessToken = await requirePortalAccessToken();\n  if (!accessToken) {\n    return safeJson(\n      { error: { code: \"unauthorized\", message: \"Sign in required.\" } },\n      401\n    );\n  }\n\n  const params = new URL(request.url).searchParams;\n  const contact = (params.get(\"contact\") || \"\").trim();\n  if (!contact || contact.length > 100) {\n    return safeJson(\n      { error: { code: \"bad_request\", message: \"contact is required.\" } },\n      400\n    );\n  }\n\n  try {\n    const result = await getChurnScore(accessToken, contact);\n    if (result === null) {\n      return safeJson(\n        { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n        503\n      );\n    }\n    return safeJson(result, 200);\n  } catch (error) {\n    if (error instanceof ControlPlaneRequestError && error.isUnauthorized) {\n      return safeJson(\n        { error: { code: \"unauthorized\", message: \"Session expired.\" } },\n        401\n      );\n    }\n    return safeJson(\n      { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n      503\n    );\n  }\n}\n");
writeNew(WEB + "/app/api/omniflow/portal/churn/radar/route.ts", "getChurnRadar", false, "import {\n  getChurnRadar,\n  requirePortalAccessToken,\n} from \"../../../../../../lib/omniflow/portal\";\nimport {\n  noStoreHeaders,\n  safeJson,\n} from \"../../../../../../lib/omniflow/request-security\";\nimport { ControlPlaneRequestError } from \"../../../../../../lib/omniflow/control-plane\";\n\n\nexport async function GET(request: Request) {\n  const accessToken = await requirePortalAccessToken();\n  if (!accessToken) {\n    return safeJson(\n      { error: { code: \"unauthorized\", message: \"Sign in required.\" } },\n      401\n    );\n  }\n\n  const params = new URL(request.url).searchParams;\n  const rawLimit = params.get(\"limit\");\n  let limit = 10;\n  if (rawLimit !== null) {\n    const parsed = Number.parseInt(rawLimit, 10);\n    if (!Number.isFinite(parsed) || parsed <= 0) {\n      return safeJson(\n        { error: { code: \"bad_request\", message: \"limit must be a number.\" } },\n        400\n      );\n    }\n    limit = parsed;\n  }\n\n  try {\n    const result = await getChurnRadar(accessToken, limit);\n    if (result === null) {\n      return safeJson(\n        { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n        503\n      );\n    }\n    return safeJson(result, 200);\n  } catch (error) {\n    if (error instanceof ControlPlaneRequestError && error.isUnauthorized) {\n      return safeJson(\n        { error: { code: \"unauthorized\", message: \"Session expired.\" } },\n        401\n      );\n    }\n    return safeJson(\n      { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n      503\n    );\n  }\n}\n");
writeNew(WEB + "/app/api/omniflow/portal/churn/report/route.ts", "getChurnReport", false, "import {\n  getChurnReport,\n  requirePortalAccessToken,\n} from \"../../../../../../lib/omniflow/portal\";\nimport {\n  noStoreHeaders,\n  safeJson,\n} from \"../../../../../../lib/omniflow/request-security\";\nimport { ControlPlaneRequestError } from \"../../../../../../lib/omniflow/control-plane\";\n\n\nexport async function GET() {\n  const accessToken = await requirePortalAccessToken();\n  if (!accessToken) {\n    return safeJson(\n      { error: { code: \"unauthorized\", message: \"Sign in required.\" } },\n      401\n    );\n  }\n\n  try {\n    const result = await getChurnReport(accessToken);\n    if (result === null) {\n      return safeJson(\n        { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n        503\n      );\n    }\n    return safeJson(result, 200);\n  } catch (error) {\n    if (error instanceof ControlPlaneRequestError && error.isUnauthorized) {\n      return safeJson(\n        { error: { code: \"unauthorized\", message: \"Session expired.\" } },\n        401\n      );\n    }\n    return safeJson(\n      { error: { code: \"portal_unavailable\", message: \"Try again shortly.\" } },\n      503\n    );\n  }\n}\n");
writeNew(CP + "/portal_churn.py", "portal_churn", true, "\"\"\"Churn radar v2: deterministic, zero AI. Scores every chat contact 0-100\nfrom five signals the workspace already has - quiet chats, unanswered\nreplies, cold carts, slowing orders and dormant repeat buyers. Read-only:\nno new tables.\"\"\"\n\nimport logging\nfrom datetime import date, datetime, timezone\nfrom typing import Any, Dict, List, Optional\n\nfrom flask import Blueprint, jsonify, request\n\nfrom portal_auth import (\n    PortalAuthUnavailable,\n    authenticate_portal_request,\n)\nimport portal_db\n\nbp = Blueprint(\"portal_churn\", __name__, url_prefix=\"/api/v1/portal\")\n\nlogger = logging.getLogger(__name__)\n\nLINKS_TABLE = \"portal_checkout_links\"\n\nWEIGHT_QUIET = 25           # +10 more once quiet passes the severe mark\nWEIGHT_UNANSWERED = 15\nWEIGHT_COLD_CART = 20       # +10 more once the cart passes the severe mark\nWEIGHT_SPEND_DROP = 15\nWEIGHT_SLOWDOWN = 10\nWEIGHT_DORMANT = 20\n\nQUIET_DAYS = 21\nQUIET_DAYS_SEVERE = 45\nUNANSWERED_DAYS = 7\nCOLD_CART_DAYS = 7\nCOLD_CART_DAYS_SEVERE = 21\nDORMANT_ORDERS = 2\nDORMANT_DAYS = 45\nWINDOW_DAYS = 30\n\nTIER_COOLING = 40\nTIER_AT_RISK = 70\n\nMAX_SCORE = 100\nMAX_POOL = 200\nMAX_PAID = 60\nMAX_RECENT = 10\nMAX_RADAR = 50\n\n_CHURN_DDL_READY = True  # module owns no tables\n\n\ndef _principal_or_error():\n    try:\n        principal = authenticate_portal_request()\n    except PortalAuthUnavailable as error:\n        return None, (jsonify({\"error\": {\"code\": \"portal_unavailable\",\n                                         \"message\": str(error)}}), 503)\n    if principal is None:\n        return None, (jsonify({\"error\": {\"code\": \"unauthorized\",\n                                         \"message\": \"Sign in required.\"}}), 401)\n    return principal, None\n\n\ndef _table_exists(cur, table: str) -> bool:\n    cur.execute(\"SELECT to_regclass(%s) AS oid\", (table,))\n    rows = portal_db.rows(cur)\n    return bool(rows and rows[0].get(\"oid\"))\n\n\ndef _now() -> datetime:\n    return datetime.now(timezone.utc)\n\n\ndef _parse_ts(value: Any) -> Optional[datetime]:\n    if isinstance(value, datetime):\n        return value if value.tzinfo else value.replace(tzinfo=timezone.utc)\n    if isinstance(value, date):\n        return datetime(value.year, value.month, value.day,\n                        tzinfo=timezone.utc)\n    text = str(value or \"\").strip()\n    if not text:\n        return None\n    try:\n        parsed = datetime.fromisoformat(text.replace(\"Z\", \"+00:00\"))\n    except ValueError:\n        return None\n    if parsed.tzinfo is None:\n        parsed = parsed.replace(tzinfo=timezone.utc)\n    return parsed\n\n\ndef _days_since(value: Any, now: datetime) -> Optional[int]:\n    ts = _parse_ts(value)\n    if ts is None:\n        return None\n    return max(0, int((now - ts).total_seconds() // 86400))\n\n\ndef _load_recent_messages(cur, client_id, contact) -> List[Dict[str, Any]]:\n    cur.execute(\n        \"SELECT m.direction AS direction, m.created_at AS created_at\"\n        \" FROM \" + portal_db._q(portal_db.MSGS_TABLE) +\n        \" m JOIN \" + portal_db._q(portal_db.CONV_TABLE) + \" c\"\n        \" ON c.id = m.conversation_id\"\n        \" WHERE c.client_id = %s AND c.contact_id = %s\"\n        \" ORDER BY m.id DESC LIMIT \" + str(MAX_RECENT),\n        (client_id, contact),\n    )\n    return [{\"direction\": str(row.get(\"direction\") or \"\"),\n             \"created_at\": row.get(\"created_at\")}\n            for row in portal_db.rows(cur)]\n\n\ndef _load_oldest_open_cart(cur, client_id, contact) -> Any:\n    if not _table_exists(cur, LINKS_TABLE):\n        return None\n    cur.execute(\n        \"SELECT created_at FROM \" + portal_db._q(LINKS_TABLE) +\n        \" WHERE client_id = %s AND contact_id = %s AND status = 'open'\"\n        \" ORDER BY created_at ASC, id ASC LIMIT 1\",\n        (client_id, contact),\n    )\n    rows = portal_db.rows(cur)\n    return rows[0].get(\"created_at\") if rows else None\n\n\ndef _load_paid_links(cur, client_id, contact) -> List[Any]:\n    if not _table_exists(cur, LINKS_TABLE):\n        return []\n    cur.execute(\n        \"SELECT created_at FROM \" + portal_db._q(LINKS_TABLE) +\n        \" WHERE client_id = %s AND contact_id = %s AND status = 'paid'\"\n        \" ORDER BY created_at DESC, id DESC LIMIT \" + str(MAX_PAID),\n        (client_id, contact),\n    )\n    return [row.get(\"created_at\") for row in portal_db.rows(cur)]\n\n\ndef score_contact(messages: List[Dict[str, Any]], open_cart_at: Any,\n                  paid_links: List[Any], now: datetime) -> Dict[str, Any]:\n    \"\"\"Five deterministic signals -> score 0-100 with human reasons.\"\"\"\n    score = 0\n    reasons: List[str] = []\n\n    last_in_days: Optional[int] = None\n    for entry in messages:\n        if entry.get(\"direction\") != \"in\":\n            continue\n        days = _days_since(entry.get(\"created_at\"), now)\n        if days is not None:\n            last_in_days = days\n            break\n    if last_in_days is not None and last_in_days >= QUIET_DAYS:\n        score += WEIGHT_QUIET + (10 if last_in_days >= QUIET_DAYS_SEVERE\n                                 else 0)\n        reasons.append(\"No message from them in \" + str(last_in_days)\n                       + \" days\")\n\n    unanswered_days: Optional[int] = None\n    if messages:\n        head = messages[0]\n        if head.get(\"direction\") == \"out\":\n            unanswered_days = _days_since(head.get(\"created_at\"), now)\n            if unanswered_days is not None \\\n                    and unanswered_days >= UNANSWERED_DAYS:\n                score += WEIGHT_UNANSWERED\n                reasons.append(\"Your last reply is unanswered for \"\n                               + str(unanswered_days) + \" days\")\n\n    open_cart_days = _days_since(open_cart_at, now)\n    if open_cart_days is not None and open_cart_days >= COLD_CART_DAYS:\n        score += WEIGHT_COLD_CART + (10 if open_cart_days\n                                     >= COLD_CART_DAYS_SEVERE else 0)\n        reasons.append(\"Cart open for \" + str(open_cart_days) + \" days\")\n\n    order_days = [days for days in\n                  (_days_since(ts, now) for ts in paid_links)\n                  if days is not None]\n    recent = sum(1 for days in order_days if days < WINDOW_DAYS)\n    prior = sum(1 for days in order_days\n                if WINDOW_DAYS <= days < WINDOW_DAYS * 2)\n    if prior >= 1 and recent == 0:\n        score += WEIGHT_SPEND_DROP\n        reasons.append(\"No orders in the last month\")\n    elif prior >= 2 and recent < prior:\n        score += WEIGHT_SLOWDOWN\n        reasons.append(\"Orders slowed down\")\n\n    last_order_days = min(order_days) if order_days else None\n    if len(order_days) >= DORMANT_ORDERS and last_order_days is not None \\\n            and last_order_days >= DORMANT_DAYS:\n        score += WEIGHT_DORMANT\n        reasons.append(\"Hasn't reordered in \" + str(last_order_days)\n                       + \" days\")\n\n    final = min(MAX_SCORE, score)\n    if final >= TIER_AT_RISK:\n        tier = \"at_risk\"\n    elif final >= TIER_COOLING:\n        tier = \"cooling\"\n    else:\n        tier = \"healthy\"\n    return {\n        \"score\": final,\n        \"tier\": tier,\n        \"reasons\": reasons,\n        \"signals\": {\n            \"last_inbound_days\": last_in_days,\n            \"unanswered_days\": unanswered_days,\n            \"open_cart_days\": open_cart_days,\n            \"orders_last_30d\": recent,\n            \"orders_prior_30d\": prior,\n            \"paid_orders\": len(order_days),\n            \"last_order_days\": last_order_days,\n        },\n    }\n\n\ndef _score_contact(cur, client_id, contact, now: datetime) -> Dict[str, Any]:\n    messages = _load_recent_messages(cur, client_id, contact)\n    open_cart_at = _load_oldest_open_cart(cur, client_id, contact)\n    paid_links = _load_paid_links(cur, client_id, contact)\n    return score_contact(messages, open_cart_at, paid_links, now)\n\n\ndef _contact_pool(cur, client_id) -> List[Dict[str, str]]:\n    cur.execute(\n        \"SELECT contact_id, MAX(COALESCE(contact_name, '')) AS name\"\n        \" FROM \" + portal_db._q(portal_db.CONV_TABLE) +\n        \" WHERE client_id = %s AND COALESCE(contact_id, '') <> ''\"\n        \" GROUP BY contact_id ORDER BY contact_id LIMIT \" + str(MAX_POOL),\n        (client_id,),\n    )\n    return [{\"contact_id\": str(row.get(\"contact_id\") or \"\"),\n             \"name\": str(row.get(\"name\") or \"\")}\n            for row in portal_db.rows(cur)]\n\n\ndef _score_pool(cur, client_id, now: datetime) -> List[Dict[str, Any]]:\n    scored = []\n    for entry in _contact_pool(cur, client_id):\n        result = _score_contact(cur, client_id, entry[\"contact_id\"], now)\n        result[\"contact_id\"] = entry[\"contact_id\"]\n        result[\"name\"] = entry[\"name\"]\n        scored.append(result)\n    return scored\n\n\ndef _tier_counts(scored: List[Dict[str, Any]]) -> Dict[str, int]:\n    counts = {\"healthy\": 0, \"cooling\": 0, \"at_risk\": 0}\n    for entry in scored:\n        counts[entry[\"tier\"]] = counts.get(entry[\"tier\"], 0) + 1\n    return counts\n\n\n@bp.get(\"/churn/score\")\ndef churn_score():\n    principal, error = _principal_or_error()\n    if error:\n        return error\n    contact = (request.args.get(\"contact\") or \"\").strip()[:100]\n    if not contact:\n        return jsonify({\"error\": {\"code\": \"bad_request\",\n                                  \"message\": \"contact is required.\"}}), 400\n    try:\n        portal_db.ensure_tables()\n        conn = portal_db._conn()\n        try:\n            with conn.cursor() as cur:\n                result = _score_contact(cur, principal[\"client_id\"], contact,\n                                        _now())\n        finally:\n            conn.close()\n    except Exception as error:\n        logger.warning(\"churn score failed: %s\", error)\n        return jsonify(portal_db.portal_unavailable(error, \"churn score\")[0]), 503\n    result[\"contact_id\"] = contact\n    return jsonify(result), 200\n\n\n@bp.get(\"/churn/radar\")\ndef churn_radar():\n    \"\"\"Workspace-wide: the most at-risk contacts first.\"\"\"\n    principal, error = _principal_or_error()\n    if error:\n        return error\n    try:\n        limit = int(request.args.get(\"limit\") or 10)\n    except ValueError:\n        return jsonify({\"error\": {\"code\": \"bad_request\",\n                                  \"message\": \"limit must be a number.\"}}), 400\n    limit = max(1, min(limit, MAX_RADAR))\n    try:\n        portal_db.ensure_tables()\n        conn = portal_db._conn()\n        try:\n            with conn.cursor() as cur:\n                scored = _score_pool(cur, principal[\"client_id\"], _now())\n        finally:\n            conn.close()\n    except Exception as error:\n        logger.warning(\"churn radar failed: %s\", error)\n        return jsonify(portal_db.portal_unavailable(error, \"churn radar\")[0]), 503\n    flagged = [entry for entry in scored if entry[\"score\"] > 0]\n    flagged.sort(key=lambda entry: (-entry[\"score\"],\n                                    str(entry.get(\"name\") or \"\").lower()))\n    return jsonify({\n        \"contacts\": [\n            {\n                \"contact_id\": entry[\"contact_id\"],\n                \"name\": entry.get(\"name\") or \"\",\n                \"score\": entry[\"score\"],\n                \"tier\": entry[\"tier\"],\n                \"reasons\": entry[\"reasons\"][:2],\n            }\n            for entry in flagged[:limit]\n        ],\n        \"scored\": len(scored),\n        \"counts\": _tier_counts(scored),\n    }), 200\n\n\n@bp.get(\"/churn/report\")\ndef churn_report():\n    \"\"\"Workspace aggregate: tier counts, average score, top reasons.\"\"\"\n    principal, error = _principal_or_error()\n    if error:\n        return error\n    try:\n        portal_db.ensure_tables()\n        conn = portal_db._conn()\n        try:\n            with conn.cursor() as cur:\n                scored = _score_pool(cur, principal[\"client_id\"], _now())\n        finally:\n            conn.close()\n    except Exception as error:\n        logger.warning(\"churn report failed: %s\", error)\n        return jsonify(portal_db.portal_unavailable(error, \"churn report\")[0]), 503\n    histogram: Dict[str, int] = {}\n    for entry in scored:\n        for reason in entry[\"reasons\"]:\n            histogram[reason] = histogram.get(reason, 0) + 1\n    top_reasons = sorted(histogram.items(), key=lambda kv: (-kv[1], kv[0]))\n    return jsonify({\n        \"scored\": len(scored),\n        \"avg_score\": (round(sum(entry[\"score\"] for entry in scored)\n                            / len(scored), 1) if scored else None),\n        \"tiers\": _tier_counts(scored),\n        \"top_reasons\": [{\"reason\": reason, \"count\": count}\n                        for reason, count in top_reasons[:5]],\n    }), 200\n");
applySwaps(WEB + "/app/dashboard/(portal)/growth/page.tsx", "Churn radar", false, [["  priority: number;\n}\n\nconst DAYS_OPTIONS = [7, 14, 30, 60];", "  priority: number;\n}\n\ninterface ChurnRadarContact {\n  contactId: string;\n  name: string;\n  score: number;\n  tier: string;\n  reasons: string[];\n}\n\nconst DAYS_OPTIONS = [7, 14, 30, 60];"], ["  const [churn, setChurn] = useState<ChurnContact[]>([]);\n  const [days, setDays] = useState(14);", "  const [churn, setChurn] = useState<ChurnContact[]>([]);\n  const [radar, setRadar] = useState<ChurnRadarContact[]>([]);\n  const [days, setDays] = useState(14);"], ["  const load = useCallback(async () => {\n    const [churnPayload, forecast, ideas, negotiation, checkout, listenRules, listenHits, routingRules] =\n      await Promise.all([", "  const load = useCallback(async () => {\n    const [churnPayload, radarPayload, forecast, ideas, negotiation, checkout, listenRules, listenHits, routingRules] =\n      await Promise.all(["], ["          \"/api/omniflow/portal/insights/churn?days=\" + days\n        ),\n        getJson<StaffingForecast>(\"/api/omniflow/portal/insights/staffing\"),", "          \"/api/omniflow/portal/insights/churn?days=\" + days\n        ),\n        getJson<{ contacts: ChurnRadarContact[] }>(\n          \"/api/omniflow/portal/churn/radar?limit=5\"\n        ),\n        getJson<StaffingForecast>(\"/api/omniflow/portal/insights/staffing\"),"], ["    setChurn(churnPayload?.contacts ?? []);\n    setStaffing(forecast);", "    setChurn(churnPayload?.contacts ?? []);\n    setRadar(radarPayload?.contacts ?? []);\n    setStaffing(forecast);"], ["      <div className=\"space-y-4\">\n        <Section\n          title=\"Churn risk\"", "      <div className=\"space-y-4\">\n        <Section\n          title=\"Churn radar\"\n          hint=\"Scored from quiet chats, unanswered replies, cold carts and slowing orders.\"\n        >\n          {radar.length === 0 ? (\n            <p className=\"text-xs text-slate-500\">\n              No churn signals right now.\n            </p>\n          ) : (\n            <ul className=\"space-y-1.5\">\n              {radar.map((entry) => (\n                <li\n                  key={entry.contactId}\n                  className=\"flex items-center justify-between gap-2 rounded-xl border border-white/[0.06] bg-white/[0.015] px-3 py-2\"\n                >\n                  <div className=\"min-w-0\">\n                    <p className=\"truncate text-sm text-slate-200\">\n                      {entry.name || entry.contactId}\n                    </p>\n                    {entry.reasons[0] ? (\n                      <p className=\"truncate text-[10px] text-slate-500\">\n                        {entry.reasons[0]}\n                      </p>\n                    ) : null}\n                  </div>\n                  <span\n                    className={`shrink-0 rounded-md border px-1.5 py-0.5 text-[10px] ${\n                      entry.tier === \"at_risk\"\n                        ? \"border-rose-400/25 bg-rose-400/[0.08] text-rose-300\"\n                        : \"border-amber-400/25 bg-amber-400/[0.08] text-amber-300\"\n                    }`}\n                  >\n                    {entry.score} ·{\" \"}\n                    {entry.tier === \"at_risk\" ? \"at risk\" : \"cooling\"}\n                  </span>\n                </li>\n              ))}\n            </ul>\n          )}\n        </Section>\n\n        <Section\n          title=\"Churn risk\""]]);
applySwaps(WEB + "/app/dashboard/(portal)/customers/profile/ProfileClient.tsx", "churn/score?contact=", false, [["  const [recos, setRecos] = useState<RecoSuggestion[]>([]);\n\n  const load = useCallback(async () => {", "  const [recos, setRecos] = useState<RecoSuggestion[]>([]);\n  const [churn, setChurn] = useState<{ score: number; tier: string; reason: string } | null>(null);\n\n  const load = useCallback(async () => {"], ["        if (active) setRecos([]);\n      }\n    })();\n    return () => {\n      active = false;\n    };\n  }, [profile?.contactId]);\n\n  if (missing || !contact) {", "        if (active) setRecos([]);\n      }\n    })();\n    return () => {\n      active = false;\n    };\n  }, [profile?.contactId]);\n\n  useEffect(() => {\n    if (!profile?.contactId) return;\n    let active = true;\n    void (async () => {\n      try {\n        const response = await fetch(\n          \"/api/omniflow/portal/churn/score?contact=\" +\n            encodeURIComponent(profile.contactId),\n          { cache: \"no-store\" }\n        );\n        if (!response.ok) {\n          if (active) setChurn(null);\n          return;\n        }\n        const payload: unknown = await response.json().catch(() => null);\n        if (active && payload !== null && typeof payload === \"object\") {\n          const raw = payload as Record<string, unknown>;\n          const score = typeof raw.score === \"number\" ? raw.score : 0;\n          const tier = typeof raw.tier === \"string\" ? raw.tier : \"\";\n          const reasons = Array.isArray(raw.reasons)\n            ? raw.reasons.filter((reason): reason is string =>\n                typeof reason === \"string\"\n              )\n            : [];\n          if (score > 0 && (tier === \"cooling\" || tier === \"at_risk\")) {\n            setChurn({ score, tier, reason: reasons[0] ?? \"\" });\n          } else if (active) {\n            setChurn(null);\n          }\n        } else if (active) {\n          setChurn(null);\n        }\n      } catch {\n        if (active) setChurn(null);\n      }\n    })();\n    return () => {\n      active = false;\n    };\n  }, [profile?.contactId]);\n\n  if (missing || !contact) {"], ["                  Risk: {risk}\n                </span>\n              ) : null}\n            </div>\n          ) : null}\n          {profile && profile.tags.length > 0 ? (", "                  Risk: {risk}\n                </span>\n              ) : null}\n              {churn ? (\n                <span\n                  title={churn.reason}\n                  className={`rounded-md border px-1.5 py-0.5 ${\n                    churn.tier === \"at_risk\"\n                      ? \"border-rose-400/25 bg-rose-400/[0.08] text-rose-300\"\n                      : \"border-amber-400/25 bg-amber-400/[0.08] text-amber-300\"\n                  }`}\n                >\n                  Churn: {churn.tier === \"at_risk\" ? \"at risk\" : \"cooling\"} (\n                  {churn.score})\n                </span>\n              ) : null}\n            </div>\n          ) : null}\n          {profile && profile.tags.length > 0 ? ("]]);

console.log("SUMMARY: " + applied + " applied, " + already + " already done, " + warnings + " warnings");